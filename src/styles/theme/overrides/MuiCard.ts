import { palatte } from "../colors";

export const MuiCard = {
  styleOverrides: {
    root: {
      border: `solid 1px`,
      borderColor: palatte.borderPrimary,
      boxShadow: 'none',
    },
  },
};
